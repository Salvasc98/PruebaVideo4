

<div class="text-center">
    <h1 class="display-4">@ViewData["Tittle"]</h1>
    <br/>
    <br />
    @if (Model != null) {
        <select id="selAreas">
            <option value="0">Selecciona empleado</option>
            @foreach(PruebaVideo4.Models.Empleado empleado in Model){
                <option value="@empleado.Id">@empleado.Nombre</option>
            }
        </select>

    }
</div>
